/// Must match patient device id in mobile_app FirebaseService / firmware.
const String deviceId = 'neuroguard-001';

const String vitalsPath = 'devices/$deviceId/vitals';
const String eventsPath = 'devices/$deviceId/events';
